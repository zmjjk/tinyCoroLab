file(GLOB API_SOURCE_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")

add_library(
  tinycoro_lock
  OBJECT
  ${API_SOURCE_FILES}
)

set(ALL_OBJECT_FILES
  ${ALL_OBJECT_FILES} $<TARGET_OBJECTS:tinycoro_lock>
  PARENT_SCOPE)